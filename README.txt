Nimbus-JWT

README

Nimbus JWT is a JSON Web Token (JWT) implementation for Java with support for 
signatures (JWS), encryption (JWE) and web keys (JWK). 

The repository has been moved to https://bitbucket.org/nimbusds/nimbus-jwt


[eof]
